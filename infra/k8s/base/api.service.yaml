apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: rtc-apps
  labels:
    app: api
    app.kubernetes.io/part-of: rtc-platform
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 4000
      targetPort: http
