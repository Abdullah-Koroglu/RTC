apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: rtc-data
  labels:
    app: redis
    app.kubernetes.io/part-of: rtc-platform
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: redis
