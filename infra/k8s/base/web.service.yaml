apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: rtc-apps
  labels:
    app: web
    app.kubernetes.io/part-of: rtc-platform
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
